{
  "name": "@ibor/root",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "new-version": "lerna version --conventional-commits --yes",
    "test": "lerna run test",
    "diff": "lerna run diff"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
